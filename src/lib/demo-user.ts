// Placeholder account used everywhere until real Supabase Auth sessions replace it.
// A fixed, valid UUID (not "demo-user") because Profile.id / WorkoutLog.userId are uuid columns.
export const DEMO_USER_ID = "00000000-0000-0000-0000-000000000001";
